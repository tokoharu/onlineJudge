#include<iostream>
#include<cstdio>
#include<cmath>
#include<string>
#include<algorithm>

using namespace std;
typedef pair<int,int> PII;

int dist[1005];
int inf = 2000000000;

vector<PII> graph[1005];

int main() {
  int N, A,B;
  cin>>N>>A>>B;
  for(int i=0; i<A; i++) {
    
  }
    
  
  return 0;
}
