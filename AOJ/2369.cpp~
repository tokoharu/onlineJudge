#include<iostream>
#include<cstdio>
#include<cmath>
#include<string>
using namespace std;

int main() {
  string s;
  cin>>s;
  while(1) {
    bool update = false;

    if(!update) break;
  }
  if(s=="x") cout<<"Cat"<<endl;
  else       cout<<"Rabbit"<<endl;
  return 0;
  
}
